{
  "name": "dev.turtle.shortener",
  "module": "",
  "type": "module",
  "scripts": {
    "build": "bun run tasks/build.js",
    "deploy": "bun run tasks/deploy.js",
    "watch": "bun run tasks/watch.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5"
  }
}